services:
  db:
    image: postgres:17
    container_name: web-plantas
    restart: always
    environment:
      POSTGRES_DB: productos
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data2:/var/lib/postgresql/data

volumes:
  postgres_data2: